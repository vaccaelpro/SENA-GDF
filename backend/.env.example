# Configuración de Base de Datos MySQL (Producción / Aiven Cloud)
DB_HOST=mysql-xxxxxx.a.aivencloud.com
DB_USER=avnadmin
DB_PORT=23811
DB_PASSWORD=tu_password_aiven
DB_NAME=defaultdb

# Configuración de Base de Datos MySQL Local (Fallback / Docker local)
DB_HOST_LOCAL=mysql
DB_USER_LOCAL=sena_user
DB_PORT_LOCAL=3306
DB_PASSWORD_LOCAL=sena_password
DB_NAME_LOCAL=sena

# Servidor
PORT=3001
NODE_ENV=production
FRONT_URL=http://localhost:3000

# Autenticación JWT
JWT_SECRET=tu_clave_secreta_jwt_aqui
JWT_EXPIRES_IN=1h

# Notificaciones y Correos
EMAIL_USER=soporte.senagdf@gmail.com
BREVO_API_KEY=tu_api_key_brevo

# Integración IA (Opcional)
OPENROUTER_API_KEY=
OPENROUTER_MODELS=
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_SITE_NAME=SENA-GDF

GEMINI_API_KEY=
GEMINI_MODELS=
